{
  "private": true,
  "name": "fast-game-server",
  "version": "0.17.0",
  "description": "Colyseus game server template for fast-game (single node, no Redis)",
  "type": "module",
  "main": "build/index.js",
  "engines": {
    "node": ">= 20.9.0"
  },
  "scripts": {
    "start": "tsx watch src/index.ts",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "clean": "rimraf build",
    "loadtest": "tsx loadtest/example.ts --room game_test_room --numClients 2",
    "test": "mocha -r tsx ../tests/game/**/*.test.ts --exit --timeout 15000"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@colyseus/loadtest": "^0.17.7",
    "@colyseus/testing": "^0.17.10",
    "@colyseus/sdk": "^0.17.17",
    "@types/express": "^4.17.1",
    "@types/mocha": "^10.0.1",
    "mocha": "^11.7.5",
    "rimraf": "^5.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@colyseus/tools": "^0.17.0",
    "@colyseus/schema": "^4.0.0",
    "colyseus": "^0.17.6",
    "express": "^4.18.2"
  }
}
